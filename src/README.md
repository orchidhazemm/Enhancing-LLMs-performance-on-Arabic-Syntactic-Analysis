# Source code

Main code for the project:

- Preprocessing and data loading
- LLM API calls and prompt templates
- Evaluation metrics (e.g. attachment score, accuracy)
- Utilities and helpers

Use this package from `notebooks/` or `scripts/` by adding the project root to `PYTHONPATH` or installing in editable mode.
