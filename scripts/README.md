# Scripts

Utility scripts for:

- Downloading or preparing datasets
- Preprocessing pipelines
- Running evaluation or batch experiments from the command line

Example: `python scripts/run_eval.py --config experiments/config.yaml`
